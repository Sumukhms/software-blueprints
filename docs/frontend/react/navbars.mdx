---
sidebar_position: 2
title: "Navbars"
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Navbar Components

5 generic top navigation bars for any project.

## 1. Standard Top Navbar
<Tabs>
  <TabItem value="preview" label="Preview" default>
    <div style={{ padding: '20px' }}>
      <style>{`.n-std { display: flex; justify-content: space-between; align-items: center; padding: 15px 30px; background: white; border-radius: 8px; border: 1px solid #eee; } .n-std-l { font-weight: bold; font-size: 1.2rem; } .n-std-m { display: flex; gap: 20px; } .n-std-m a { text-decoration: none; color: #666; font-weight: 500; } .n-std-b { background: #111; color: white; padding: 8px 16px; border-radius: 6px; border: none; cursor: pointer; }`}</style>
      <nav className="n-std"><div className="n-std-l">Brand.</div><div className="n-std-m"><a href="#">Home</a><a href="#">Products</a><a href="#">About</a></div><button className="n-std-b">Sign In</button></nav>
    </div>
  </TabItem>
  <TabItem value="code" label="Code">

```jsx
export default function StandardNavbar() {
  return (
    <nav className="navbar-standard">
      <div className="navbar-logo">Brand.</div>
      <div className="navbar-links">
        <a href="#">Home</a>
        <a href="#">Products</a>
        <a href="#">About</a>
      </div>
      <button className="navbar-btn">Sign In</button>
    </nav>
  );
}
```

```css
.navbar-standard {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 15px 30px;
  background: white;
  border-radius: 8px;
  border: 1px solid #eee;
}
.navbar-logo {
  font-weight: bold;
  font-size: 1.2rem;
}
.navbar-links {
  display: flex;
  gap: 20px;
}
.navbar-links a {
  text-decoration: none;
  color: #666;
  font-weight: 500;
}
.navbar-btn {
  background: #111;
  color: white;
  padding: 8px 16px;
  border-radius: 6px;
  border: none;
  cursor: pointer;
}
```

  </TabItem>
</Tabs>

## 2. Glassmorphism Navbar
<Tabs>
  <TabItem value="preview" label="Preview" default>
    <div style={{ padding: '20px', background: 'linear-gradient(135deg, #4f46e5, #ec4899)', borderRadius: '12px', minHeight: '150px' }}>
      <style>{`.n-gls { display: flex; justify-content: space-between; align-items: center; padding: 15px 30px; background: rgba(255, 255, 255, 0.1); backdrop-filter: blur(10px); border: 1px solid rgba(255, 255, 255, 0.2); border-radius: 12px; color: white; } .n-gls-m a { color: rgba(255, 255, 255, 0.8); margin: 0 10px; text-decoration: none; }`}</style>
      <nav className="n-gls"><div style={{fontWeight:'bold', letterSpacing:'1px'}}>VISION.</div><div className="n-gls-m"><a href="#">Showcase</a><a href="#">Studio</a></div><button style={{background:'rgba(255,255,255,0.2)', color:'white', border:'1px solid rgba(255,255,255,0.3)', padding:'8px 20px', borderRadius:'20px'}}>Enter</button></nav>
    </div>
  </TabItem>
  <TabItem value="code" label="Code">

```jsx
export default function GlassNavbar() {
  return (
    <nav className="navbar-glass">
      <div className="glass-logo">VISION.</div>
      <div className="glass-links">
        <a href="#">Showcase</a>
        <a href="#">Studio</a>
      </div>
      <button className="glass-btn">Enter</button>
    </nav>
  );
}
```

```css
.navbar-glass {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 15px 30px;
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 12px;
  color: white;
}
.glass-logo {
  font-weight: bold;
  letter-spacing: 1px;
}
.glass-links a {
  color: rgba(255, 255, 255, 0.8);
  margin: 0 10px;
  text-decoration: none;
}
.glass-btn {
  background: rgba(255,255,255,0.2);
  color: white;
  border: 1px solid rgba(255,255,255,0.3);
  padding: 8px 20px;
  border-radius: 20px;
  cursor: pointer;
}
```

  </TabItem>
</Tabs>

## 3. Centered Logo Navbar
<Tabs>
  <TabItem value="preview" label="Preview" default>
    <div style={{ padding: '20px' }}>
      <style>{`.n-ctr { display: grid; grid-template-columns: 1fr auto 1fr; align-items: center; padding: 15px 30px; background: white; border-radius: 8px; border: 1px solid #eee; } .n-ctr-logo { font-weight: 800; font-size: 1.5rem; color: #4f46e5; text-align: center; } .n-ctr-left, .n-ctr-right { display: flex; gap: 20px; } .n-ctr-right { justify-content: flex-end; } .n-ctr a { color: #666; text-decoration: none; font-weight: 500; }`}</style>
      <nav className="n-ctr">
        <div className="n-ctr-left"><a href="#">Shop</a><a href="#">Collections</a></div>
        <div className="n-ctr-logo">LUXE.</div>
        <div className="n-ctr-right"><a href="#">Search</a><a href="#">Cart (0)</a></div>
      </nav>
    </div>
  </TabItem>
  <TabItem value="code" label="Code">

```jsx
export default function CenteredNavbar() {
  return (
    <nav className="navbar-centered">
      <div className="nav-left">
        <a href="#">Shop</a>
        <a href="#">Collections</a>
      </div>
      <div className="nav-logo">LUXE.</div>
      <div className="nav-right">
        <a href="#">Search</a>
        <a href="#">Cart (0)</a>
      </div>
    </nav>
  );
}
```

```css
.navbar-centered {
  display: grid;
  grid-template-columns: 1fr auto 1fr;
  align-items: center;
  padding: 15px 30px;
  background: white;
  border-radius: 8px;
  border: 1px solid #eee;
}
.nav-logo {
  font-weight: 800;
  font-size: 1.5rem;
  color: #4f46e5;
  text-align: center;
}
.nav-left, .nav-right {
  display: flex;
  gap: 20px;
}
.nav-right {
  justify-content: flex-end;
}
.navbar-centered a {
  color: #666;
  text-decoration: none;
  font-weight: 500;
}
```

  </TabItem>
</Tabs>

## 4. Minimalist Search Navbar
<Tabs>
  <TabItem value="preview" label="Preview" default>
    <div style={{ padding: '20px' }}>
      <style>{`.n-min { display: flex; justify-content: space-between; align-items: center; padding: 10px 20px; background: white; border-bottom: 2px solid #000; } .n-min-srch { padding: 8px 16px; border: 1px solid #ccc; border-radius: 20px; width: 300px; background: #f9f9f9; }`}</style>
      <nav className="n-min">
        <strong style={{fontSize:'1.2rem'}}>The Daily.</strong>
        <input type="text" className="n-min-srch" placeholder="Search articles..." />
        <div style={{display:'flex', gap:'15px'}}><span style={{fontWeight:'bold'}}>Login</span></div>
      </nav>
    </div>
  </TabItem>
  <TabItem value="code" label="Code">

```jsx
export default function SearchNavbar() {
  return (
    <nav className="navbar-minimal">
      <strong className="nav-brand">The Daily.</strong>
      <input type="text" className="nav-search" placeholder="Search articles..." />
      <div className="nav-actions">
        <span className="nav-login">Login</span>
      </div>
    </nav>
  );
}
```

```css
.navbar-minimal {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 10px 20px;
  background: white;
  border-bottom: 2px solid #000;
}
.nav-brand {
  font-size: 1.2rem;
}
.nav-search {
  padding: 8px 16px;
  border: 1px solid #ccc;
  border-radius: 20px;
  width: 300px;
  background: #f9f9f9;
}
.nav-actions {
  display: flex;
  gap: 15px;
}
.nav-login {
  font-weight: bold;
  cursor: pointer;
}
```

  </TabItem>
</Tabs>

## 5. Two-Tier Enterprise Navbar
<Tabs>
  <TabItem value="preview" label="Preview" default>
    <div style={{ padding: '20px' }}>
      <style>{`.n-tier { background: white; border: 1px solid #eee; border-radius: 8px; overflow: hidden; } .n-tier-top { background: #111; color: #ccc; padding: 5px 20px; display: flex; justify-content: flex-end; gap: 15px; font-size: 0.8rem; } .n-tier-bot { padding: 15px 20px; display: flex; justify-content: space-between; align-items: center; }`}</style>
      <nav className="n-tier">
        <div className="n-tier-top"><span>Support</span><span>Sales: 1-800-555-0198</span></div>
        <div className="n-tier-bot">
          <strong style={{color:'#4f46e5', fontSize:'1.3rem'}}>CloudFlow</strong>
          <div style={{display:'flex', gap:'20px', fontWeight:'500'}}><span style={{color:'#666'}}>Products</span><span style={{color:'#666'}}>Solutions</span><span style={{color:'#666'}}>Pricing</span></div>
          <button style={{background:'#4f46e5', color:'white', padding:'8px 16px', border:'none', borderRadius:'4px'}}>Console</button>
        </div>
      </nav>
    </div>
  </TabItem>
  <TabItem value="code" label="Code">

```jsx
export default function TwoTierNavbar() {
  return (
    <nav className="navbar-tier">
      <div className="tier-top">
        <span>Support</span>
        <span>Sales: 1-800-555-0198</span>
      </div>
      <div className="tier-bottom">
        <strong className="tier-brand">CloudFlow</strong>
        <div className="tier-links">
          <span>Products</span>
          <span>Solutions</span>
          <span>Pricing</span>
        </div>
        <button className="tier-btn">Console</button>
      </div>
    </nav>
  );
}
```

```css
.navbar-tier {
  background: white;
  border: 1px solid #eee;
  border-radius: 8px;
  overflow: hidden;
}
.tier-top {
  background: #111;
  color: #ccc;
  padding: 5px 20px;
  display: flex;
  justify-content: flex-end;
  gap: 15px;
  font-size: 0.8rem;
}
.tier-bottom {
  padding: 15px 20px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}
.tier-brand {
  color: #4f46e5;
  font-size: 1.3rem;
}
.tier-links {
  display: flex;
  gap: 20px;
  font-weight: 500;
  color: #666;
}
.tier-links span { cursor: pointer; }
.tier-btn {
  background: #4f46e5;
  color: white;
  padding: 8px 16px;
  border: none;
  border-radius: 4px;
  cursor: pointer;
}
```

  </TabItem>
</Tabs>
