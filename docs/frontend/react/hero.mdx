---
sidebar_position: 5
title: "Hero Sections"
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Hero Sections

Enterprise-grade hero sections using semantic `<section>` tags, strict heading hierarchy (`<h1>`), and mobile responsiveness.

## 1. Modern Gradient Hero
<Tabs>
  <TabItem value="preview" label="Preview" default>
    <div style={{ fontFamily: 'system-ui, sans-serif' }}>
      <style>
        {`
          .hero-gradient { padding: 80px 20px; text-align: center; background: linear-gradient(135deg, #fdfbfb 0%, #ebedee 100%); border-radius: 16px; }
          .hero-title { font-size: clamp(2rem, 5vw, 3rem); font-weight: 800; margin: 0 0 20px; background: linear-gradient(to right, #4f46e5, #ec4899); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
          .hero-subtitle { font-size: 1.2rem; color: #666; max-width: 600px; margin: 0 auto 30px; }
          .hero-btn { background: #111; color: white; padding: 15px 30px; border-radius: 30px; border: none; font-weight: bold; cursor: pointer; outline: none; }
          .hero-btn:focus-visible { box-shadow: 0 0 0 3px rgba(17,17,17,0.4); }
        `}
      </style>
      <section className="hero-gradient" aria-labelledby="hero-title-1">
        <h1 id="hero-title-1" className="hero-title">Build Faster.</h1>
        <p className="hero-subtitle">Stop rewriting the same generic UI components over and over. Copy, paste, and launch your next big idea today.</p>
        <button type="button" className="hero-btn">Get Started</button>
      </section>
    </div>
  </TabItem>
  <TabItem value="code" label="Code">

```jsx
export default function GradientHero() {
  return (
    <section className="hero-gradient" aria-labelledby="hero-title-1">
      <h1 id="hero-title-1" className="hero-title">Build Faster.</h1>
      <p className="hero-subtitle">Stop rewriting the same generic UI components over and over. Copy, paste, and launch your next big idea today.</p>
      <button type="button" className="hero-btn">Get Started</button>
    </section>
  );
}
```

```css
.hero-gradient {
  padding: 80px 20px;
  text-align: center;
  background: linear-gradient(135deg, #fdfbfb 0%, #ebedee 100%);
  border-radius: 16px;
}
.hero-title {
  /* clamp() automatically scales font size for mobile */
  font-size: clamp(2rem, 5vw, 3rem);
  font-weight: 800;
  margin: 0 0 20px;
  background: linear-gradient(to right, #4f46e5, #ec4899);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}
.hero-subtitle {
  font-size: 1.2rem;
  color: #666;
  max-width: 600px;
  margin: 0 auto 30px;
}
.hero-btn {
  background: #111;
  color: white;
  padding: 15px 30px;
  border-radius: 30px;
  border: none;
  font-weight: bold;
  cursor: pointer;
  outline: none;
}
.hero-btn:focus-visible {
  box-shadow: 0 0 0 3px rgba(17,17,17,0.4);
}
```

  </TabItem>
</Tabs>

## 2. Split-Screen Hero
<Tabs>
  <TabItem value="preview" label="Preview" default>
    <div style={{ fontFamily: 'system-ui, sans-serif' }}>
      <style>
        {`
          .hero-split { display: flex; align-items: center; background: white; border-radius: 16px; border: 1px solid #eee; overflow: hidden; }
          .hero-content { flex: 1; padding: 40px; }
          .hero-title-split { font-size: clamp(2rem, 4vw, 2.5rem); font-weight: bold; color: #111; margin: 0 0 15px; line-height: 1.2; }
          .hero-text { color: #666; margin-bottom: 20px; }
          .hero-btn-split { background: #4f46e5; color: white; padding: 12px 24px; border: none; border-radius: 8px; font-weight: bold; cursor: pointer; outline: none; }
          .hero-btn-split:focus-visible { box-shadow: 0 0 0 3px rgba(79, 70, 229, 0.4); }
          .hero-image { flex: 1; background: #e5e7eb; height: 300px; display: flex; align-items: center; justify-content: center; color: #9ca3af; font-size: 2rem; }
          @media (max-width: 768px) { .hero-split { flex-direction: column; } .hero-image { width: 100%; height: 200px; } .hero-content { padding: 20px; text-align: center; } }
        `}
      </style>
      <section className="hero-split">
        <div className="hero-content">
          <h1 className="hero-title-split">The ultimate workspace.</h1>
          <p className="hero-text">Organize your entire life with our beautiful, intuitive platform.</p>
          <button type="button" className="hero-btn-split">Download App</button>
        </div>
        <div className="hero-image" role="img" aria-label="Product Showcase Screenshot">
          Product Image
        </div>
      </section>
    </div>
  </TabItem>
  <TabItem value="code" label="Code">

```jsx
export default function SplitHero() {
  return (
    <section className="hero-split">
      <div className="hero-content">
        <h1 className="hero-title-split">The ultimate workspace.</h1>
        <p className="hero-text">Organize your entire life with our beautiful, intuitive platform.</p>
        <button type="button" className="hero-btn-split">Download App</button>
      </div>
      <div className="hero-image" role="img" aria-label="Product Showcase Screenshot">
        Product Image
      </div>
    </section>
  );
}
```

```css
.hero-split {
  display: flex;
  align-items: center;
  background: white;
  border-radius: 16px;
  border: 1px solid #eee;
  overflow: hidden;
}
.hero-content {
  flex: 1;
  padding: 40px;
}
.hero-title-split {
  font-size: clamp(2rem, 4vw, 2.5rem);
  font-weight: bold;
  color: #111;
  margin: 0 0 15px;
  line-height: 1.2;
}
.hero-text { color: #666; margin-bottom: 20px; }
.hero-btn-split {
  background: #4f46e5;
  color: white;
  padding: 12px 24px;
  border: none;
  border-radius: 8px;
  font-weight: bold;
  cursor: pointer;
  outline: none;
}
.hero-btn-split:focus-visible { box-shadow: 0 0 0 3px rgba(79, 70, 229, 0.4); }
.hero-image {
  flex: 1;
  background: #e5e7eb;
  height: 300px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #9ca3af;
  font-size: 2rem;
}
@media (max-width: 768px) {
  .hero-split { flex-direction: column; }
  .hero-image { width: 100%; height: 200px; }
  .hero-content { padding: 20px; text-align: center; }
}
```

  </TabItem>
</Tabs>

## 3. Minimalist Typography Hero
<Tabs>
  <TabItem value="preview" label="Preview" default>
    <div style={{ fontFamily: 'system-ui, sans-serif' }}>
      <style>
        {`
          .hero-minimal { padding: 100px 20px; text-align: center; background: white; }
          .hero-min-title { font-size: clamp(3rem, 8vw, 4rem); font-weight: 900; letter-spacing: -2px; color: #000; margin: 0 0 20px; line-height: 1; }
          .hero-min-btn { background: transparent; color: #000; border: 2px solid #000; padding: 15px 40px; font-weight: bold; font-size: 1.2rem; cursor: pointer; transition: all 0.2s; outline: none; }
          .hero-min-btn:hover { background: #000; color: white; }
          .hero-min-btn:focus-visible { outline: 3px solid #000; outline-offset: 4px; }
        `}
      </style>
      <section className="hero-minimal">
        <h1 className="hero-min-title">Less, but better.</h1>
        <p style={{fontSize:'1.5rem', color:'#888', marginBottom:'40px'}}>Design that gets out of your way.</p>
        <button type="button" className="hero-min-btn">See Portfolio</button>
      </section>
    </div>
  </TabItem>
  <TabItem value="code" label="Code">

```jsx
export default function MinimalHero() {
  return (
    <section className="hero-minimal">
      <h1 className="hero-min-title">Less, but better.</h1>
      <p className="hero-subtitle-large">Design that gets out of your way.</p>
      <button type="button" className="hero-min-btn">See Portfolio</button>
    </section>
  );
}
```

```css
.hero-minimal {
  padding: 100px 20px;
  text-align: center;
  background: white;
}
.hero-min-title {
  font-size: clamp(3rem, 8vw, 4rem);
  font-weight: 900;
  letter-spacing: -2px;
  color: #000;
  margin: 0 0 20px;
  line-height: 1;
}
.hero-subtitle-large {
  font-size: 1.5rem;
  color: #888;
  margin-bottom: 40px;
}
.hero-min-btn {
  background: transparent;
  color: #000;
  border: 2px solid #000;
  padding: 15px 40px;
  font-weight: bold;
  font-size: 1.2rem;
  cursor: pointer;
  transition: all 0.2s;
  outline: none;
}
.hero-min-btn:hover { background: #000; color: white; }
.hero-min-btn:focus-visible { outline: 3px solid #000; outline-offset: 4px; }
```

  </TabItem>
</Tabs>

## 4. Pattern Background Hero
<Tabs>
  <TabItem value="preview" label="Preview" default>
    <div style={{ fontFamily: 'system-ui, sans-serif' }}>
      <style>
        {`
          .hero-pattern { padding: 80px 20px; text-align: center; border-radius: 12px; background-color: #f3f4f6; background-image: radial-gradient(#cbd5e1 2px, transparent 2px); background-size: 20px 20px; }
          .hero-card { background: white; padding: 40px; border-radius: 16px; max-width: 600px; margin: 0 auto; box-shadow: 0 10px 25px rgba(0,0,0,0.05); }
          .hero-card h1 { margin: 0 0 15px; font-size: clamp(1.8rem, 4vw, 2.5rem); }
          .hero-btn-pat { background: #10b981; color: white; padding: 12px 24px; border: none; border-radius: 6px; font-weight: bold; cursor: pointer; outline: none; }
          .hero-btn-pat:focus-visible { box-shadow: 0 0 0 3px rgba(16, 185, 129, 0.4); }
          @media (max-width: 480px) { .hero-card { padding: 20px; } }
        `}
      </style>
      <section className="hero-pattern">
        <div className="hero-card">
          <h1>Scale your team instantly.</h1>
          <p style={{color:'#666', marginBottom:'20px'}}>Hire top-tier developers in under 48 hours.</p>
          <button type="button" className="hero-btn-pat">Find Talent</button>
        </div>
      </section>
    </div>
  </TabItem>
  <TabItem value="code" label="Code">

```jsx
export default function PatternHero() {
  return (
    <section className="hero-pattern">
      <div className="hero-card">
        <h1>Scale your team instantly.</h1>
        <p>Hire top-tier developers in under 48 hours.</p>
        <button type="button" className="hero-btn-pat">Find Talent</button>
      </div>
    </section>
  );
}
```

```css
.hero-pattern {
  padding: 80px 20px;
  text-align: center;
  border-radius: 12px;
  background-color: #f3f4f6;
  background-image: radial-gradient(#cbd5e1 2px, transparent 2px);
  background-size: 20px 20px;
}
.hero-card {
  background: white;
  padding: 40px;
  border-radius: 16px;
  max-width: 600px;
  margin: 0 auto;
  box-shadow: 0 10px 25px rgba(0,0,0,0.05);
}
.hero-card h1 { 
  margin: 0 0 15px; 
  font-size: clamp(1.8rem, 4vw, 2.5rem);
}
.hero-card p { color: #666; margin-bottom: 20px; }
.hero-btn-pat {
  background: #10b981;
  color: white;
  padding: 12px 24px;
  border: none;
  border-radius: 6px;
  font-weight: bold;
  cursor: pointer;
  outline: none;
}
.hero-btn-pat:focus-visible { box-shadow: 0 0 0 3px rgba(16, 185, 129, 0.4); }

@media (max-width: 480px) {
  .hero-card { padding: 20px; }
}
```

  </TabItem>
</Tabs>

## 5. Dark Image Hero
<Tabs>
  <TabItem value="preview" label="Preview" default>
    <div style={{ fontFamily: 'system-ui, sans-serif' }}>
      <style>
        {`
          .hero-dark { padding: 100px 20px; text-align: center; background: #111; color: white; border-radius: 12px; position: relative; overflow: hidden; }
          .hero-overlay { position: absolute; inset: 0; background: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.8)); z-index: 1; }
          .hero-drk-c { position: relative; z-index: 2; }
          .hero-drk-title { font-size: clamp(2.5rem, 6vw, 3.5rem); font-weight: bold; margin: 0 0 20px; }
          .hero-drk-btn { background: white; color: #111; padding: 15px 30px; border: none; border-radius: 4px; font-weight: bold; font-size: 1.1rem; cursor: pointer; outline: none; }
          .hero-drk-btn:focus-visible { outline: 3px solid #4f46e5; outline-offset: 4px; }
        `}
      </style>
      <section className="hero-dark">
        <div className="hero-overlay"></div>
        <div className="hero-drk-c">
          <h1 className="hero-drk-title">Enter the next dimension.</h1>
          <button type="button" className="hero-drk-btn">Watch Trailer</button>
        </div>
      </section>
    </div>
  </TabItem>
  <TabItem value="code" label="Code">

```jsx
export default function DarkHero() {
  return (
    <section className="hero-dark">
      <div className="hero-overlay"></div>
      <div className="hero-content-z">
        <h1 className="hero-drk-title">Enter the next dimension.</h1>
        <button type="button" className="hero-drk-btn">Watch Trailer</button>
      </div>
    </section>
  );
}
```

```css
.hero-dark {
  padding: 100px 20px;
  text-align: center;
  background: #111; /* Add your image URL here: url('bg.jpg') */
  background-size: cover;
  background-position: center;
  color: white;
  border-radius: 12px;
  position: relative;
  overflow: hidden;
}
.hero-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.8));
  z-index: 1;
}
.hero-content-z {
  position: relative;
  z-index: 2;
}
.hero-drk-title {
  font-size: clamp(2.5rem, 6vw, 3.5rem);
  font-weight: bold;
  margin: 0 0 20px;
}
.hero-drk-btn {
  background: white;
  color: #111;
  padding: 15px 30px;
  border: none;
  border-radius: 4px;
  font-weight: bold;
  font-size: 1.1rem;
  cursor: pointer;
  outline: none;
}
.hero-drk-btn:focus-visible {
  outline: 3px solid #4f46e5;
  outline-offset: 4px;
}
```

  </TabItem>
</Tabs>
