---
sidebar_position: 6
title: "Forms & Auth"
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Forms & Authentication

Enterprise-grade forms featuring semantic `<form>` wrappers, strict accessibility labeling (`htmlFor` + `id`), and clear interactive focus states.

## 1. Dark Mode Login
<Tabs>
  <TabItem value="preview" label="Preview" default>
    <div style={{ fontFamily: 'system-ui, sans-serif', padding: '20px', background: '#0a0a0a', borderRadius: '12px', display: 'flex', justifyContent: 'center' }}>
      <style>{`.auth-dark { background: #171717; padding: 40px; border-radius: 12px; border: 1px solid #333; width: 100%; max-width: 400px; color: white; } .form-group { margin-bottom: 20px; } .form-group label { display: block; color: #a3a3a3; font-size: 0.9rem; margin-bottom: 8px; cursor: pointer; } .form-group input { width: 100%; padding: 12px; border-radius: 6px; background: #262626; border: 1px solid #404040; color: white; box-sizing: border-box; outline: none; transition: border-color 0.2s; } .form-group input:focus-visible { border-color: #4f46e5; box-shadow: 0 0 0 2px rgba(79, 70, 229, 0.2); } .auth-btn { width: 100%; padding: 12px; background: #4f46e5; color: white; border: none; border-radius: 6px; font-weight: bold; cursor: pointer; outline: none; } .auth-btn:focus-visible { box-shadow: 0 0 0 3px rgba(255,255,255,0.4); }`}</style>
      <form className="auth-dark" onSubmit={(e)=>e.preventDefault()}>
        <h2 style={{margin:'0 0 20px'}}>Sign In</h2>
        <div className="form-group">
          <label htmlFor="email-1">Email</label>
          <input id="email-1" type="email" placeholder="you@example.com" required aria-required="true" />
        </div>
        <div className="form-group">
          <label htmlFor="pass-1">Password</label>
          <input id="pass-1" type="password" placeholder="••••••••" required aria-required="true" />
        </div>
        <button type="submit" className="auth-btn">Sign In</button>
      </form>
    </div>
  </TabItem>
  <TabItem value="code" label="Code">

```jsx
export default function DarkLogin() {
  return (
    <form className="auth-dark" onSubmit={(e) => e.preventDefault()}>
      <h2>Sign In</h2>
      <div className="form-group">
        <label htmlFor="email-login">Email</label>
        <input 
          id="email-login" 
          type="email" 
          placeholder="you@example.com" 
          required 
          aria-required="true"
        />
      </div>
      <div className="form-group">
        <label htmlFor="password-login">Password</label>
        <input 
          id="password-login" 
          type="password" 
          placeholder="••••••••" 
          required 
          aria-required="true"
        />
      </div>
      <button type="submit" className="auth-btn">Sign In</button>
    </form>
  );
}
```

```css
.auth-dark {
  background: #171717;
  padding: 40px;
  border-radius: 12px;
  border: 1px solid #333;
  width: 100%;
  max-width: 400px;
  color: white;
  box-sizing: border-box;
}
.auth-dark h2 { margin: 0 0 20px; }
.form-group { margin-bottom: 20px; }
.form-group label {
  display: block;
  color: #a3a3a3;
  font-size: 0.9rem;
  margin-bottom: 8px;
  cursor: pointer;
}
.form-group input {
  width: 100%;
  padding: 12px;
  border-radius: 6px;
  background: #262626;
  border: 1px solid #404040;
  color: white;
  box-sizing: border-box;
  outline: none;
  transition: border-color 0.2s;
}
.form-group input:focus-visible { 
  border-color: #4f46e5; 
  box-shadow: 0 0 0 2px rgba(79, 70, 229, 0.2);
}
.auth-btn {
  width: 100%;
  padding: 12px;
  background: #4f46e5;
  color: white;
  border: none;
  border-radius: 6px;
  font-weight: bold;
  cursor: pointer;
  outline: none;
}
.auth-btn:focus-visible { box-shadow: 0 0 0 3px rgba(255,255,255,0.4); }
```

  </TabItem>
</Tabs>

## 2. Modern Signup Card
<Tabs>
  <TabItem value="preview" label="Preview" default>
    <div style={{ fontFamily: 'system-ui, sans-serif', padding: '20px', background: '#f3f4f6', borderRadius: '12px', display: 'flex', justifyContent: 'center' }}>
      <style>{`.signup-card { background: white; padding: 40px; border-radius: 16px; box-shadow: 0 10px 25px rgba(0,0,0,0.05); width: 100%; max-width: 400px; box-sizing: border-box; } .signup-input { width: 100%; padding: 12px; border-radius: 8px; border: 1px solid #e5e7eb; margin-bottom: 20px; box-sizing: border-box; background: #f9fafb; outline: none; } .signup-input:focus-visible { border-color: #111; box-shadow: 0 0 0 2px rgba(17,17,17,0.1); } .signup-btn { width: 100%; padding: 12px; background: #111; color: white; border: none; border-radius: 8px; font-weight: bold; cursor: pointer; outline: none; } .signup-btn:focus-visible { box-shadow: 0 0 0 3px rgba(79, 70, 229, 0.4); }`}</style>
      <form className="signup-card" onSubmit={(e)=>e.preventDefault()}>
        <h2 style={{margin:'0 0 5px', color:'#111'}}>Create Account</h2>
        <p style={{color:'#666', margin:'0 0 20px'}}>Join 50,000+ developers.</p>
        <label htmlFor="name-2" className="sr-only" style={{display:'none'}}>Full Name</label>
        <input id="name-2" type="text" className="signup-input" placeholder="Full Name" aria-label="Full Name" />
        <label htmlFor="email-2" className="sr-only" style={{display:'none'}}>Email</label>
        <input id="email-2" type="email" className="signup-input" placeholder="Email address" aria-label="Email address" />
        <label htmlFor="pass-2" className="sr-only" style={{display:'none'}}>Password</label>
        <input id="pass-2" type="password" className="signup-input" placeholder="Password" aria-label="Password" />
        <button type="submit" className="signup-btn">Create Account</button>
      </form>
    </div>
  </TabItem>
  <TabItem value="code" label="Code">

```jsx
export default function LightSignup() {
  return (
    <form className="signup-card" onSubmit={(e) => e.preventDefault()}>
      <h2>Create Account</h2>
      <p className="signup-sub">Join 50,000+ developers.</p>
      
      {/* Visually hidden labels for screen readers */}
      <label htmlFor="signup-name" className="sr-only">Full Name</label>
      <input id="signup-name" type="text" className="signup-input" placeholder="Full Name" required />
      
      <label htmlFor="signup-email" className="sr-only">Email address</label>
      <input id="signup-email" type="email" className="signup-input" placeholder="Email address" required />
      
      <label htmlFor="signup-pass" className="sr-only">Password</label>
      <input id="signup-pass" type="password" className="signup-input" placeholder="Password" required />
      
      <button type="submit" className="signup-btn">Create Account</button>
    </form>
  );
}
```

```css
.signup-card {
  background: white;
  padding: 40px;
  border-radius: 16px;
  box-shadow: 0 10px 25px rgba(0,0,0,0.05);
  width: 100%;
  max-width: 400px;
  box-sizing: border-box;
}
.signup-card h2 { margin: 0 0 5px; color: #111; }
.signup-sub { color: #666; margin: 0 0 20px; }
.sr-only {
  position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px;
  overflow: hidden; clip: rect(0, 0, 0, 0); white-space: nowrap; border-width: 0;
}
.signup-input {
  width: 100%;
  padding: 12px;
  border-radius: 8px;
  border: 1px solid #e5e7eb;
  margin-bottom: 20px;
  box-sizing: border-box;
  background: #f9fafb;
  outline: none;
}
.signup-input:focus-visible { 
  border-color: #111; 
  box-shadow: 0 0 0 2px rgba(17,17,17,0.1);
}
.signup-btn {
  width: 100%;
  padding: 12px;
  background: #111;
  color: white;
  border: none;
  border-radius: 8px;
  font-weight: bold;
  cursor: pointer;
  outline: none;
}
.signup-btn:focus-visible { box-shadow: 0 0 0 3px rgba(79, 70, 229, 0.4); }
```

  </TabItem>
</Tabs>

## 3. Inline Newsletter Form
<Tabs>
  <TabItem value="preview" label="Preview" default>
    <div style={{ fontFamily: 'system-ui, sans-serif' }}>
      <style>{`.news-inline { display: flex; gap: 10px; max-width: 500px; } .news-input { flex: 1; padding: 12px 20px; border: 1px solid #d1d5db; border-radius: 30px; outline: none; } .news-input:focus-visible { border-color: #4f46e5; box-shadow: 0 0 0 2px rgba(79,70,229,0.2); } .news-btn { padding: 12px 25px; background: #4f46e5; color: white; border: none; border-radius: 30px; font-weight: bold; cursor: pointer; outline: none; } .news-btn:focus-visible { box-shadow: 0 0 0 3px rgba(17,17,17,0.3); } @media (max-width: 500px) { .news-inline { flex-direction: column; } }`}</style>
      <form className="news-inline" onSubmit={(e)=>e.preventDefault()}>
        <label htmlFor="nl-email" className="sr-only" style={{display:'none'}}>Newsletter Email</label>
        <input id="nl-email" type="email" className="news-input" placeholder="Enter your email..." aria-label="Newsletter Email" />
        <button type="submit" className="news-btn">Subscribe</button>
      </form>
    </div>
  </TabItem>
  <TabItem value="code" label="Code">

```jsx
export default function NewsletterInline() {
  return (
    <form className="news-inline" onSubmit={(e) => e.preventDefault()}>
      <label htmlFor="newsletter-email" className="sr-only">Newsletter Email</label>
      <input 
        id="newsletter-email" 
        type="email" 
        className="news-input" 
        placeholder="Enter your email..." 
        required 
      />
      <button type="submit" className="news-btn">Subscribe</button>
    </form>
  );
}
```

```css
.news-inline {
  display: flex;
  gap: 10px;
  max-width: 500px;
}
.sr-only {
  position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px;
  overflow: hidden; clip: rect(0, 0, 0, 0); white-space: nowrap; border-width: 0;
}
.news-input {
  flex: 1;
  padding: 12px 20px;
  border: 1px solid #d1d5db;
  border-radius: 30px;
  outline: none;
}
.news-input:focus-visible { 
  border-color: #4f46e5; 
  box-shadow: 0 0 0 2px rgba(79,70,229,0.2);
}
.news-btn {
  padding: 12px 25px;
  background: #4f46e5;
  color: white;
  border: none;
  border-radius: 30px;
  font-weight: bold;
  cursor: pointer;
  outline: none;
}
.news-btn:focus-visible { box-shadow: 0 0 0 3px rgba(17,17,17,0.3); }

@media (max-width: 500px) {
  .news-inline { flex-direction: column; }
}
```

  </TabItem>
</Tabs>

## 4. Contact Us Card
<Tabs>
  <TabItem value="preview" label="Preview" default>
    <div style={{ fontFamily: 'system-ui, sans-serif' }}>
      <style>{`.contact-card { background: white; padding: 30px; border: 1px solid #eee; border-radius: 12px; max-width: 500px; box-sizing: border-box; } .contact-row { display: flex; gap: 15px; } .contact-input, .contact-textarea { width: 100%; padding: 12px; border: 1px solid #ddd; border-radius: 6px; margin-bottom: 15px; box-sizing: border-box; outline: none; } .contact-input:focus-visible, .contact-textarea:focus-visible { border-color: #111; box-shadow: 0 0 0 2px rgba(17,17,17,0.1); } .contact-textarea { height: 120px; resize: vertical; } .contact-btn { padding: 12px 24px; background: #111; color: white; border: none; border-radius: 6px; font-weight: bold; cursor: pointer; outline: none; } .contact-btn:focus-visible { box-shadow: 0 0 0 3px rgba(79,70,229,0.4); } @media (max-width: 500px) { .contact-row { flex-direction: column; gap: 0; } }`}</style>
      <form className="contact-card" onSubmit={(e)=>e.preventDefault()}>
        <h3 style={{margin:'0 0 20px'}}>Send us a message</h3>
        <div className="contact-row">
          <input type="text" aria-label="First Name" className="contact-input" placeholder="First Name" />
          <input type="text" aria-label="Last Name" className="contact-input" placeholder="Last Name" />
        </div>
        <input type="email" aria-label="Email" className="contact-input" placeholder="Email" />
        <textarea aria-label="Message" className="contact-textarea" placeholder="How can we help?"></textarea>
        <button type="submit" className="contact-btn">Send Message</button>
      </form>
    </div>
  </TabItem>
  <TabItem value="code" label="Code">

```jsx
export default function ContactForm() {
  return (
    <form className="contact-card" onSubmit={(e) => e.preventDefault()}>
      <h3>Send us a message</h3>
      <div className="contact-row">
        <div>
          <label htmlFor="contact-fname" className="sr-only">First Name</label>
          <input id="contact-fname" type="text" className="contact-input" placeholder="First Name" required />
        </div>
        <div>
          <label htmlFor="contact-lname" className="sr-only">Last Name</label>
          <input id="contact-lname" type="text" className="contact-input" placeholder="Last Name" required />
        </div>
      </div>
      
      <label htmlFor="contact-email" className="sr-only">Email</label>
      <input id="contact-email" type="email" className="contact-input" placeholder="Email" required />
      
      <label htmlFor="contact-msg" className="sr-only">Message</label>
      <textarea id="contact-msg" className="contact-textarea" placeholder="How can we help?" required></textarea>
      
      <button type="submit" className="contact-btn">Send Message</button>
    </form>
  );
}
```

```css
.contact-card {
  background: white;
  padding: 30px;
  border: 1px solid #eee;
  border-radius: 12px;
  max-width: 500px;
  box-sizing: border-box;
}
.contact-card h3 { margin: 0 0 20px; }
.sr-only {
  position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px;
  overflow: hidden; clip: rect(0, 0, 0, 0); white-space: nowrap; border-width: 0;
}
.contact-row {
  display: flex;
  gap: 15px;
}
.contact-row > div { flex: 1; }
.contact-input, .contact-textarea {
  width: 100%;
  padding: 12px;
  border: 1px solid #ddd;
  border-radius: 6px;
  margin-bottom: 15px;
  box-sizing: border-box;
  outline: none;
}
.contact-input:focus-visible, .contact-textarea:focus-visible { 
  border-color: #111; 
  box-shadow: 0 0 0 2px rgba(17,17,17,0.1);
}
.contact-textarea {
  height: 120px;
  resize: vertical; /* Allow only vertical resize for forms */
}
.contact-btn {
  padding: 12px 24px;
  background: #111;
  color: white;
  border: none;
  border-radius: 6px;
  font-weight: bold;
  cursor: pointer;
  outline: none;
}
.contact-btn:focus-visible { box-shadow: 0 0 0 3px rgba(79,70,229,0.4); }

@media (max-width: 500px) {
  .contact-row { flex-direction: column; gap: 0; }
}
```

  </TabItem>
</Tabs>

## 5. Password Reset Request
<Tabs>
  <TabItem value="preview" label="Preview" default>
    <div style={{ fontFamily: 'system-ui, sans-serif' }}>
      <style>{`.reset-card { max-width: 400px; padding: 40px; text-align: center; border: 1px solid #eee; border-radius: 16px; background: white; box-sizing: border-box; width: 100%; } .reset-input { width: 100%; padding: 12px; text-align: center; border: 1px solid #ddd; border-radius: 8px; margin: 20px 0; font-size: 1.1rem; box-sizing: border-box; outline: none; } .reset-input:focus-visible { border-color: #4f46e5; box-shadow: 0 0 0 2px rgba(79,70,229,0.2); } .reset-btn { width: 100%; padding: 12px; background: #4f46e5; color: white; border: none; border-radius: 8px; font-weight: bold; cursor: pointer; outline: none; } .reset-btn:focus-visible { box-shadow: 0 0 0 3px rgba(17,17,17,0.3); }`}</style>
      <form className="reset-card" onSubmit={(e)=>e.preventDefault()}>
        <h2 style={{margin:'0 0 10px'}}>Forgot Password?</h2>
        <p style={{color:'#666', margin:0}}>Enter your email and we'll send you a recovery link.</p>
        <label htmlFor="reset-email" className="sr-only" style={{display:'none'}}>Account Email</label>
        <input id="reset-email" type="email" className="reset-input" placeholder="hello@example.com" aria-label="Account Email" />
        <button type="submit" className="reset-btn">Send Link</button>
        <p style={{marginTop:'20px', fontSize:'0.9rem'}}><a href="#" style={{color:'#4f46e5', textDecoration:'none', outline:'none'}}>Back to Login</a></p>
      </form>
    </div>
  </TabItem>
  <TabItem value="code" label="Code">

```jsx
export default function PasswordReset() {
  return (
    <form className="reset-card" onSubmit={(e) => e.preventDefault()}>
      <h2>Forgot Password?</h2>
      <p className="reset-text">Enter your email and we'll send you a recovery link.</p>
      
      <label htmlFor="reset-email" className="sr-only">Account Email</label>
      <input 
        id="reset-email" 
        type="email" 
        className="reset-input" 
        placeholder="hello@example.com" 
        required
      />
      <button type="submit" className="reset-btn">Send Link</button>
      
      <p className="reset-back"><a href="#">Back to Login</a></p>
    </form>
  );
}
```

```css
.reset-card {
  max-width: 400px;
  width: 100%;
  padding: 40px;
  text-align: center;
  border: 1px solid #eee;
  border-radius: 16px;
  background: white;
  box-sizing: border-box;
}
.reset-card h2 { margin: 0 0 10px; }
.reset-text { color: #666; margin: 0; }
.sr-only {
  position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px;
  overflow: hidden; clip: rect(0, 0, 0, 0); white-space: nowrap; border-width: 0;
}
.reset-input {
  width: 100%;
  padding: 12px;
  text-align: center;
  border: 1px solid #ddd;
  border-radius: 8px;
  margin: 20px 0;
  font-size: 1.1rem;
  box-sizing: border-box;
  outline: none;
}
.reset-input:focus-visible { 
  border-color: #4f46e5; 
  box-shadow: 0 0 0 2px rgba(79,70,229,0.2);
}
.reset-btn {
  width: 100%;
  padding: 12px;
  background: #4f46e5;
  color: white;
  border: none;
  border-radius: 8px;
  font-weight: bold;
  cursor: pointer;
  outline: none;
}
.reset-btn:focus-visible { box-shadow: 0 0 0 3px rgba(17,17,17,0.3); }
.reset-back { margin-top: 20px; font-size: 0.9rem; }
.reset-back a { 
  color: #4f46e5; 
  text-decoration: none; 
  font-weight: 500;
  outline: none;
  border-radius: 2px;
}
.reset-back a:focus-visible { box-shadow: 0 0 0 2px rgba(17,17,17,0.3); }
```

  </TabItem>
</Tabs>
