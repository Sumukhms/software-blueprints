---
sidebar_position: 2
title: "UI Templates"
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# AI SaaS UI Templates

When building an AI Document Analyzer, a drag-and-drop file uploader with progress state is the most critical UI component.

## 1. Document Upload Area

<Tabs>
  <TabItem value="preview" label="Preview" default>
    <div style={{ fontFamily: 'system-ui, sans-serif', padding: '20px' }}>
      <style>
        {`
          .upload-zone {
            border: 2px dashed #6366f1;
            border-radius: 12px;
            padding: 40px;
            text-align: center;
            background: rgba(99, 102, 241, 0.05);
            color: #333;
            transition: all 0.3s ease;
            cursor: pointer;
          }
          .upload-zone:hover {
            background: rgba(99, 102, 241, 0.1);
            border-color: #4f46e5;
          }
          .icon {
            font-size: 40px;
            margin-bottom: 15px;
          }
          .upload-text {
            font-weight: 600;
            font-size: 1.1rem;
            color: #111;
            margin: 0;
          }
          .sub-text {
            color: #666;
            font-size: 0.9rem;
            margin-top: 5px;
          }
          .upload-btn {
            margin-top: 20px;
            background: #111;
            color: white;
            padding: 10px 24px;
            border-radius: 8px;
            border: none;
            font-weight: bold;
            cursor: pointer;
          }
        `}
      </style>

      <div className="upload-zone" onClick={() => alert('File picker opened!')}>
        <div className="icon">📄</div>
        <p className="upload-text">Click or drag a PDF here to upload</p>
        <p className="sub-text">Maximum file size 50MB.</p>
        <button className="upload-btn">Browse Files</button>
      </div>
    </div>
  </TabItem>
  <TabItem value="code" label="Code">

```jsx
function FileUploader() {
  return (
    <div className="upload-zone">
      <div className="icon">📄</div>
      <p className="upload-text">Click or drag a PDF here to upload</p>
      <p className="sub-text">Maximum file size 50MB.</p>
      <button className="upload-btn">Browse Files</button>
    </div>
  );
}
```

```css
/* CSS Styles */
.upload-zone {
  border: 2px dashed #6366f1;
  border-radius: 12px;
  padding: 40px;
  text-align: center;
  background: rgba(99, 102, 241, 0.05);
  transition: all 0.3s ease;
  cursor: pointer;
}
.upload-zone:hover {
  background: rgba(99, 102, 241, 0.1);
  border-color: #4f46e5;
}
.icon {
  font-size: 40px;
  margin-bottom: 15px;
}
.upload-text {
  font-weight: 600;
  font-size: 1.1rem;
  color: #111;
  margin: 0;
}
.sub-text {
  color: #666;
  font-size: 0.9rem;
  margin-top: 5px;
}
.upload-btn {
  margin-top: 20px;
  background: #111;
  color: white;
  padding: 10px 24px;
  border-radius: 8px;
  border: none;
  font-weight: bold;
  cursor: pointer;
}
```

  </TabItem>
</Tabs>
